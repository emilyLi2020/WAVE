ANTHROPIC_API_KEY=
NEXT_PUBLIC_APP_NAME=WAVE

# Supabase — SSR / browser (publishable key is safe in the client bundle)
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Supabase — server-only for /api/db/* WAVE tables (optional; 503 → localStorage fallback)
SUPABASE_SECRET_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
